# PROJECT.md — 部材手配管理台帳プロジェクト 基本規約

このファイルはプロジェクトの前提・決定事項・作業規約をまとめたもの。
チャットを新規に立て直した場合は、まずこのファイルを読み込ませて文脈を復旧すること。

最終更新: 2026-08-12 / 版: 0.8

---

## 1. 目的

部署内の **部材手配管理台帳** をAI活用で構築する。

### 求める機能

1. **ファイル格納**: Boxを使用（部署の文化に合わせる）
2. **メイン台帳**: **SharePointリスト**（環境調査で確定。§7 参照）
   - Box上のExcel共同編集は不採用（§9 参照）
   - Boxは成果物ファイルの格納先、および各HTMLツールの配布先として使う
3. **Teams連携**
   - 台帳に所定の書き込みをした際、Teamsへメンション通知を飛ばす
   - Teams上で特定の回答をすると、台帳に反映される（双方向）
   - → アダプティブカードで実現。動作確認済み（`docs/環境台帳.md` §H）
4. **ノウハウ集への接続**
   - 台帳は「タスク管理・期限表示」だけでなく、作業ノウハウ集にも接続する
   - ノウハウ集からのOutlookメール自動出力
   - フォームによる情報入力
   - → `docs/要件.md` §4 で定義済み。**別の単一HTMLとしてBoxに配置**、
     Outlookメールは **mailto方式**、ノウハウ投稿は **Microsoft Formsの事前入力方式**

### 依存する製品

Box / SharePoint / Teams / Power Automate / Outlook / Forms

### 成果物の構成

| ファイル | 役割 | 配布 |
|---|---|---|
| **起票ツール** `kihyou.html` | 起票、明細作成、分割、JSON出力 | 全員。バージョン管理下（§7） |
| **マスタ編集ツール** `master_editor.html` | マスタJSONの作成・編集 | 管理者のみ |
| **ノウハウ集** (HTML) | 手順の参照、メール生成、ノウハウ投稿 | 全員。気軽に更新 |

**マスタの更新は、起票ツールの管理者モード（`?admin=1`）で完結する。**
マスタ編集ツールでJSONを作り、それを起票ツールに貼り付けて新版を書き出す。
部材数・部材群数・フォーマットの要素数が変わっても、HTMLの改修は不要。

---

## 2. 環境の制約（このプロジェクト最大の前提）

### 3つの環境

| 環境 | 役割 | 使えるAI |
|---|---|---|
| **自宅PC / スマホ** | 設計・実装 | Claude（Opus）チャット + Claude Code |
| **GitHub (public)** | 受け渡しの中継地 | — |
| **会社PC** | 実行・検証 | Claude Cowork |

自宅環境と会社環境は**直接連携できない**。

### 往路（自宅 → 会社）: 太い

```
Claudeが出力 → 端末で保存 → GitHubへpush → 会社PCでZIP取得 → 人が配置
```

- GitHubリポジトリは **public・平文**（会社PCで個人アカウントにログイン不可のため）
- **暗号化ZIPは採用しない**。代わりに徹底した匿名化で守る（→ §4）
- **Cowork自身はGitHubに到達できない**。人が落として配置する

### 復路（会社 → 自宅）: 極細

- **写真撮影のみ**。データファイルの持ち出しは不可
- 会社環境から GitHub へのアップロードは **絶対に行わない**（データ持ち出しにあたる）
- GitHubは **一方通行（自宅→会社）専用**

### 往復頻度

スマホが自宅環境側なので、**会社にいる間も1日数往復可能**。
→ 分岐を事前に読み切る必要は薄い。ライブで対話しながら進める方が速い。

---

## 3. 役割分担

### 自宅側（頭）

| | 担当 |
|---|---|
| **Claudeチャット** | 設計の議論、方針判断、**写真の読み取りと診断**、指示文の作成 |
| **Claude Code** | リポジトリ実装、複数ファイルの一括更新、コミット／push |

**分ける基準**: 迷っている作業はチャット、決まった作業はCode。
写真は必ずチャット側へ投げる。診断結果を受けてCodeに指示する流れ。

### 会社側

**ブラウザ操作を伴う調査は、すべて人間が行う。** Coworkはブラウザを操作できない。

| | 担当 |
|---|---|
| **人間** | M365/Box/Teamsの画面操作、設定、撮影 |
| **Cowork** | ファイルの作成・配置、ローカルファイルの読み取りと要約、ドキュメントへの追記 |

**Coworkにコードを書かせない。** 往路が太くAI（Opus）が自宅にある以上、
Coworkが実装する理由がない。書かせると会社側にだけ存在する変更が生まれ、
次の配布で静かに上書き事故が起きる。

会社側のファイルは **毎回上書きされる消耗品**。マスターは常にGitHubリポジトリ。

---

## 4. 匿名化ルール（publicリポジトリのため厳守）

会社名・部署名・実在の案件名・個人名・社内システム固有名・社内URLは**リポジトリに書かない**。

| 実物 | 表記 |
|---|---|
| 会社名 | 書かない |
| 部署名 | `{{DEPT}}` |
| SharePointサイト名・URL | `{{SP_SITE}}` |
| Teamsチーム／チャネル名 | `{{TEAM}}` / `{{CHANNEL}}` |
| Boxフォルダパス | `{{BOX_PATH}}` |
| 案件名 | `案件A`, `案件B` |
| 人名 | `担当X`, `承認者Y` |
| メールアドレス | `{{MAIL_X}}` |
| 部材の品番・品名 | `部材X`, `品名A` |

- 具体値は会社側の `config.local.md` に集約し、**リポジトリには上げない**
- **実データのマスタを埋め込んだHTMLは、リポジトリに置かない。**
  管理者モードで書き出したファイルには、氏名・メールアドレス・実在の品番・URLが含まれる。
  配布先にのみ置く。リポジトリに置くのは**サンプルマスタ入りのファイルのみ**
- **会社環境のスクリーンショットもリポジトリに含めない**（`img/` は `.gitignore`）
- push前に人間の目で匿名化を確認する（publicなので事故ると取り消せない）

---

## 5. リポジトリ構成

```
/
├─ PROJECT.md           … このファイル。プロジェクトの憲法
├─ CLAUDE.md            … Claude Code への作業指示
├─ README.md            … 現在地（今どの版で何を検証中か）
├─ docs/
│   ├─ 要件.md          … 何を作るか
│   ├─ 環境台帳.md      … 判明した会社環境の仕様を蓄積【最重要資産】
│   └─ 手順.md          … 会社で実行する番号付き手順
├─ src/                 … ソース一式（単一ファイルHTMLに寄せる）
└─ config.example.md    … 埋めるべき値の雛形
```

### 運用ルール

- 会社側は常に **フルセット取得**（トップから Download ZIP）。個別ファイルを拾わない
- 症状報告時は必ず版数を添える
- 外部依存ゼロで作る（CDN参照・pip install は社内プロキシで死ぬ可能性が高い）
- **`img/` は `.gitignore` に含める**（会社環境のスクリーンショット置き場）

---

## 6. 1サイクルの流れ

```
1. Claudeがファイル一式を出力／Codeがリポジトリを更新
2. GitHubへpush
3. 会社PCでZIP取得 → 人が配置
4. 人が手順を実行する（ブラウザ操作は人間、ファイル操作はCowork）
5. 想定どおりなら次の塊へ
6. 詰まったら画面を撮影 → チャットへ投げる
7. 判明した環境仕様を 環境台帳.md へ反映（Claudeが更新版を出力）
8. 1へ戻る
```

### 撮影のコツ

- 1枚＝1画面。スクロールが必要なら少し重ねて分割
- **コードの丸ごと撮影は避ける**（1文字の誤読が致命傷。コードは復路に流さない）
- 業務データが写る箇所はダミーに差し替える
- **画面上部の環境表示・サイト名・アドレスバー・机上の書類が写り込まないよう注意する**

### 撮るべきもの（自宅では絶対に分からないもの）

M365 UIに実在する選択肢名 / Power Automateの動的コンテンツ一覧 /
SharePoint列の内部名 / エラー全文 / 権限拒否ダイアログ

---

## 7. 環境調査（第1・第2サイクルで解決した事項）

判定根拠・撮影記録・再利用可能な判定手法は `docs/環境台帳.md` を参照。

### 台帳の置き場所: **SharePointリストに確定**

- Person型・参照（Lookup）型が使え、`要件.md` §2 の3層構造を実現できる
- SharePointトリガーが標準で揃い、通知の自動化も標準ライセンスで実現可能
- カスタム権限レベルの作成も可能（論理削除の運用に使う）
- 起票ID列に **一意制約（「固有の値を適用する」）** を設定でき、重複を物理的に防げる

### Power Automate: **プレミアムが必要なのは「HTTP 要求の受信時」のみ**

それ以外（SharePoint／Box／Teams／Forms／スケジュール）は標準で利用可能。
**`SharePoint に HTTP 要求を送信します` も標準**のため、リスト構築の自動化も可能。

起票UI（ローカルHTML）から直接フローを呼び出せないため、**ファイル方式で代替する**。

```
起票UI → JSONファイル → SharePointライブラリへ配置
  → 標準トリガーが発火 → SharePoint に HTTP 要求を送信します
  → 親・明細・タスクの3リストを一括生成
```

即時反映が必要な場合は、**ライブラリでファイルを選択して手動でフローを実行**する
（`選択したファイルの場合` トリガーは標準）。

### Teams双方向: **可**

「アダプティブ カードを投稿して応答を待機する」は標準。
**選択肢・日付・自由記述の3種類が動作し、値が正しく返ることを実機で確認済み。**
`ユーザーの @mention トークンを取得する` も利用可能。

### ローカルHTML: **可**

Box Drive経由で開ける。JavaScriptもクリップボード解析も動作する。
**mailtoによるOutlook起動**（日本語・改行・記号すべて正常）、
**HTML間の相対リンク＋日本語URLパラメータ**も確認済み。

### 往路: **成立**

github.com へのアクセス・ZIPの取得と展開・Coworkによるローカルファイル読み込み、いずれも可。

### 判明した制約

**グリッドビューへの貼り付けは実用に耐えない。**
Person型列への貼り付けでブラウザが応答不能になる（2台のPCで再現）。
担当者は必ずPerson型で持つ設計のため、方式として成立しない。
→ 詳細と代替手段は `docs/環境台帳.md` §X

### 未解決事項

`docs/環境台帳.md` の「未確認事項」に集約した。重複を避けるため本書では列挙しない。

---

## 8. 設計上の指針

- **環境依存を薄いアダプタに隔離する**。ロジック（自宅で検証可能）と、
  Teams/M365を叩く部分（会社でしか試せない）を分離する
- **自宅ではモックで動かす**。固定値でロジックを完全にテストしておく
- **単一ファイルHTMLに寄せる**（会社側での展開が楽、外部依存も避けやすい）
- **マスタは実装に埋め込まず、データとして持つ**。業務整理が進むたびに
  マスタを更新するだけで済む形にする
- **文面・案内文もマスタ化する**。実際のメニュー名やパスが想定と違っても
  会社側で修正でき、往路を消費しない

---

## 9. 検討したが採用しなかった案

| 案 | 不採用の理由 |
|---|---|
| private リポジトリ | 会社PCで個人GitHubアカウントにログイン不可 |
| 暗号化ZIPをpublicに置く | GitHubの利点（差分・履歴・Web閲覧・Claudeによる直接読み取り）が全て失われる。ファイル名が漏れる形式もある。一度上げると取り消せない。匿名化で代替可能 |
| 会社→GitHub の逆方向アップロード | データ持ち出しにあたる。方向が逆 |
| 復路をQR等で太くする | 「画面を見て記録する」から「持ち出す仕組みを作った」に性質が変わり、規程上まずい |
| 網羅的な結果ID体系の事前設計 | 往復が1日数回可能になったため過剰投資。ライブ対話で足りる |
| Coworkに実装させる | 会社側にだけ存在する変更が生まれ、上書き事故を招く |
| マスターを自宅PCに置く | 日中に自宅PCへ触れない。GitHubリポジトリをマスターとする |
| **Box上のExcel台帳** | Power Automateの標準トリガーとの相性、および3層構造の実現性でSharePointリストが優位 |
| **社内品番を部材のキーにする** | 表記ゆれ・重複があり、明細の紐づけが破綻する。内部コードを別に持つ |
| **仮コードのフラグ列** | 品番の空欄で判別できるため冗長。二重管理の原因になる |
| 起票UIが外部JSONファイルからマスタを読む方式 | 外部ファイルが必要と理解していない利用者がいるため、単体で動作しない仕組みは採用できない |
| マスタのローカル一時編集機能 | 各利用者のマスタが分岐し、バージョン管理と衝突する。またローカルHTMLのパスワードは防御として機能しない |
| 受付リスト方式（1行貼り付けてフローが展開） | 構成は単純になるが即時性が失われる |
| 起票の出力ファイル投入口をBoxにする | Box Driveの同期遅延が加わるため。SharePointライブラリを使う。ただし各HTMLツールの配布先はBox |
| **グリッドビューへの貼り付け** | **Person型列でブラウザが応答不能になる（2台のPCで再現）。担当者は必ずPerson型で持つ設計のため方式として成立しない。即時性は手動フロー実行で代替可能** |
| **ノウハウ集をBox上のExcelファイルとして作る** | 分岐ナビ、mailtoによるメール自動生成、前提条件の表示が実現できない |
| **ノウハウ集を起票ツールに統合する** | 更新頻度が違う。ノウハウ集は頻繁に更新するが、起票ツールは許可バージョン管理下にあり運用が回らない |
| **マスタを手でJSON編集する** | フォーマットの並びは要素数が多く、1つずれても気づけない。最も事故が起きやすい場所を最も手作業に頼る形にしてはならない。マスタ編集ツールで半自動化する |
| **起票ツール内でマスタを一時編集する** | 各利用者のマスタが分岐し、バージョン管理と衝突する |
| **手配主担当も未定にできるようにする** | 誰も動かないタスクが生まれる。ただし管理者モードでのみ未定を許す |
| **分割結果の確認を自動で完了扱いにする** | 素通りできてしまい、確認が形骸化する。明示的な操作を必須とし、明細や納品日が変わったら確認を失効させる |
| **作業中データの保存・再開機能** | 起票は数分で終わることが目的。中断・再開を前提にすると設計が重くなる |

---

## 10. チャット運用の決め事

- **チャットは使い捨て、リポジトリが記憶**。設計の節目ごとに決定事項を
  `docs/` へ落としてコミットする
- 会話が長くなったら遠慮なく新チャットを立て、PROJECT.md と関連ドキュメントを読ませて復帰する
- **Codeに投げるべき作業は、チャット側（Claude）から明示的にコメントする**
- 判断が要る局面はチャット、手を動かすだけならCode
- **Claudeは一度読んだURLの内容を会話中キャッシュする。**
  クエリ文字列（`?v=2`）を変えるだけでは効かない場合がある。
  **パス部分の異なるURLを渡すこと。**
  例: `/main/PROJECT.md` → `/refs/heads/main/PROJECT.md`
- **大きな変更は、Codeに部分編集させるのではなくファイル全体を差し替える。**
  部分編集は指示の解釈違いで漏れが生じやすい

---

## 11. 成果物の提出方針

### 11.1 開発中の文書

本リポジトリの文書は、**環境の前提（3つの環境、往路・復路、役割分担）を含んだまま維持する**。
これらは設計判断の根拠であり、削ると「なぜそうしたか」が失われるため。

### 11.2 提出版の作成

**プロジェクト完了時、`docs/` をクローンし、環境に関する記述を除去した提出版を作る。**

除去の対象:

- 3つの環境に関する記述（§2）
- 役割分担のうち、環境に依存する部分（§3）
- 往路・復路、GitHub経由の受け渡しに関する記述
- 不採用案のうち、環境の制約が理由のもの（§9）

残すもの: 要件、設計判断とその理由（環境に依存しないもの）、
マスタの構造、運用手順、調査で判明した製品の仕様。

### 11.3 成果物のHTML

**配布するHTMLには、環境を示唆する記述を一切含めない。**
コメント・画面上の文言のいずれも対象とする。

サンプルマスタに実在の値を入れないことと合わせて、
**成果物単体では開発の経緯が分からない状態**を保つ。
