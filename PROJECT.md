# PROJECT.md — 作業管理台帳プロジェクト 基本規約

このファイルはプロジェクトの前提・決定事項・作業規約をまとめたもの。
チャットを新規に立て直した場合は、まずこのファイルを読み込ませて文脈を復旧すること。

最終更新: 2026-08-12 / 版: 0.5

---

## 1. 目的

部署内の **作業管理台帳** をAI活用で構築する。

### 求める機能

1. **ファイル格納**: Boxを使用（部署の文化に合わせる）
2. **メイン台帳**: SharePointリスト または Box上のExcel共同編集（**未確定・調査待ち**）
3. **Teams連携**
   - 台帳に所定の書き込みをした際、Teamsへメンション通知を飛ばす
   - Teams上で特定の回答をすると、台帳に反映される（双方向）
4. **ノウハウ集への接続**
   - 台帳は「タスク管理・期限表示」だけでなく、作業ノウハウ集にも接続する
   - ノウハウ集からのOutlookメール自動出力
   - フォームによる情報入力

### 依存する製品

Box / SharePoint / Teams / Power Automate / Outlook / Forms

---

## 2. 環境の制約（このプロジェクト最大の前提）

### 3つの環境

| 環境 | 役割 | 使えるAI |
|---|---|---|
| **自宅PC / スマホ** | 設計・実装 | Claude（Opus）チャット + Claude Code |
| **GitHub (public)** | 受け渡しの中継地 | — |
| **会社PC** | 実行・検証 | Claude Cowork |

自宅環境と会社環境は**直接連携できない**。

### 往路（自宅 → 会社）: 太い

```
Claudeが出力 → 端末で保存 → GitHubへpush → 会社PCでZIP取得 → 人が配置
```

- GitHubリポジトリは **public・平文**（会社PCで個人アカウントにログイン不可のため）
- **暗号化ZIPは採用しない**。代わりに徹底した匿名化で守る（→ §4）
- **Cowork自身はGitHubに到達できない**。人が落として配置する

### 復路（会社 → 自宅）: 極細

- **写真撮影のみ**。データファイルの持ち出しは不可
- 会社環境から GitHub へのアップロードは **絶対に行わない**（データ持ち出しにあたる）
- GitHubは **一方通行（自宅→会社）専用**

### 往復頻度

スマホが自宅環境側なので、**会社にいる間も1日数往復可能**。
→ 分岐を事前に読み切る必要は薄い。ライブで対話しながら進める方が速い。

---

## 3. 役割分担

### 自宅側（頭）

| | 担当 |
|---|---|
| **Claudeチャット** | 設計の議論、方針判断、**写真の読み取りと診断**、Coworkへの指示文作成 |
| **Claude Code** | リポジトリ実装、複数ファイルの一括更新、コミット／push、Release作成 |

**分ける基準**: 迷っている作業はチャット、決まった作業はCode。
写真は必ずチャット側へ投げる。診断結果を受けてCodeに指示する流れ。

### 会社側（手と目）

**Coworkにコードを書かせない。** 往路が太くAI（Opus）が自宅にある以上、Coworkが実装する理由がない。書かせると会社側にだけ存在する変更が生まれ、次の配布で静かに上書き事故が起きる。

- **やらせること**: ファイル配置、実行、M365/Box/Teams操作、UI・権限の調査、エラー再現、結果の整形出力、`環境台帳.md` への追記
- **やらせないこと**: コード修正、設計判断、「気を利かせた」補完

会社側のファイルは **毎回上書きされる消耗品**。マスターは常にGitHubリポジトリ。

---

## 4. 匿名化ルール（publicリポジトリのため厳守）

会社名・部署名・実在の案件名・個人名・社内システム固有名・社内URLは**リポジトリに書かない**。

| 実物 | 表記 |
|---|---|
| 会社名 | 書かない |
| 部署名 | `{{DEPT}}` |
| Teamsチーム／チャネル名 | `{{TEAM}}` / `{{CHANNEL}}` |
| Boxフォルダパス | `{{BOX_PATH}}` |
| SharePointサイトURL | `{{SP_SITE}}` |
| 案件名 | `案件A`, `案件B` |
| 人名 | `担当X`, `承認者Y` |
| メールアドレス | `{{MAIL_X}}` |

- 具体値は会社側の `config.local.md` に集約し、**リポジトリには上げない**
- push前に人間の目で匿名化を確認する（publicなので事故ると取り消せない）

---

## 5. リポジトリ構成

```
/
├─ PROJECT.md           … このファイル。プロジェクトの憲法
├─ CLAUDE.md            … Claude Code への作業指示
├─ README.md            … 現在地（今どの版で何を検証中か）
├─ docs/
│   ├─ 要件.md
│   ├─ 設計.md
│   ├─ 環境台帳.md      … 判明した会社環境の仕様を蓄積【最重要資産】
│   └─ 手順.md          … 会社で実行する番号付き手順
├─ src/                 … ソース一式（単一ファイルHTMLに寄せる）
└─ config.example.md    … 埋めるべき値の雛形
```

### 運用ルール

- 会社側は常に **フルセット取得**（トップから Download ZIP）。個別ファイルを拾わない
- 版の固定は **Releases** を使う（ブラウザのみで作成可、リポジトリが太らない）
- 症状報告時は必ず版数を添える（例: `v0.3で〜`）
- 外部依存ゼロで作る（CDN参照・pip install は社内プロキシで死ぬ可能性が高い）

---

## 6. 1サイクルの流れ

```
1. Claudeがファイル一式を出力／Codeがリポジトリを更新
2. GitHubへpush・Release作成
3. 会社PCでZIP取得 → 人が配置
4. Coworkに実行させる（編集させない）
5. 想定どおりなら次の塊へ
6. 詰まったら画面を撮影 → チャットへ投げる
7. 判明した環境仕様をCoworkに 環境台帳.md へ追記させる
8. 1へ戻る
```

### 撮影のコツ

- 1枚＝1画面。スクロールが必要なら少し重ねて分割
- ターミナル・エディタはフォントを大きくしてから撮る
- **コードの丸ごと撮影は避ける**（1文字の誤読が致命傷。コードは復路に流さない）
- 業務データが写る箇所はダミーに差し替える

### 撮るべきもの（自宅では絶対に分からないもの）

M365 UIに実在する選択肢名 / Power Automateの動的コンテンツ一覧 / SharePoint列の内部名 / エラー全文 / 権限拒否ダイアログ

---

## 7. 環境調査（第1サイクルで解決した事項）

台帳の置き場所を決める分岐点だった。**第1サイクルで解決済み。** 判定根拠・撮影記録は `docs/環境台帳.md` を参照。

### 解決済み

- **台帳の置き場所: SharePointリストに確定**
  - Person型・参照（Lookup）型が使え、要件.md §2 の3層構造を実現できる
  - SharePointトリガーが標準で揃い、通知の自動化・Teams双方向の書き戻しも標準ライセンスで実現可能
  - カスタム権限レベルの作成も可能（論理削除の運用に使う）
  - Boxは成果物ファイルの格納先として併用する
- **Power Automate のプレミアムコネクタ権限**: 「**HTTP 要求の受信時**」トリガーのみプレミアムで不可。それ以外（SharePoint／Box／Teams／スケジュール）は標準で利用可能
  → 起票UI（ローカルHTML）から直接フローを呼び出すことはできないため、**ファイル投下方式で代替する**
    - v1: 起票UIがタブ区切りテキストを出力 → 人がグリッドビューに貼り付け
    - v2: 起票UIがJSONファイルを出力 → Box/SharePointの所定フォルダに配置 → 標準トリガーが発火 →「SharePoint に HTTP 要求を送信します」で3層構造を一括生成（**標準ライセンスで実現可能と確認済み**）
- **Teamsからの書き戻し**: 可（アダプティブカードを投稿して応答を待機するアクションは標準）
- **往路の成立**: github.com へのアクセス・ZIPの取得と展開・Coworkによるローカルファイル読み込み、いずれも可

### 未解決事項

残った調査項目は `docs/環境台帳.md` の「次サイクルで確認すべきこと」に集約した。重複を避けるため本書では列挙しない → 参照: `docs/環境台帳.md`

---

## 8. 設計上の指針

- **環境依存を薄いアダプタに隔離する**。ロジック（自宅で検証可能）と、Teams/M365を叩く部分（会社でしか試せない）を分離し、後者は数十行の1ファイルに押し込む
- **自宅ではモックで動かす**。APIレスポンス形状は公開ドキュメントから引き、固定値でロジックを完全にテストしておく
- **v1はローカル完結＋手動連携に寄せる**。いきなり自動化を狙わず、まず整形結果を出してTeamsには貼るだけ。自動化はv2で
- **単一ファイルHTMLに寄せる**（会社側での展開が楽、外部依存も避けやすい）

---

## 9. 検討したが採用しなかった案

| 案 | 不採用の理由 |
|---|---|
| private リポジトリ | 会社PCで個人GitHubアカウントにログイン不可 |
| 暗号化ZIPをpublicに置く | GitHubの利点（差分・履歴・Web閲覧・Claudeによる直接読み取り）が全て失われる。ファイル名が漏れる形式もある。一度上げると取り消せない。匿名化で代替可能 |
| 会社→GitHub の逆方向アップロード | データ持ち出しにあたる。方向が逆 |
| 復路をQR等で太くする | 「画面を見て記録する」から「持ち出す仕組みを作った」に性質が変わり、規程上まずい |
| 網羅的な結果ID体系の事前設計 | 往復が1日数回可能になったため過剰投資。ライブ対話で足りる |
| Coworkに実装させる | 会社側にだけ存在する変更が生まれ、上書き事故を招く |
| マスターを自宅PCに置く | 日中に自宅PCへ触れない。GitHubリポジトリをマスターとする |
| 起票UIが外部JSONファイルからマスタを読む方式 | 外部ファイルが必要と理解していない利用者がいるため、単体で動作しない仕組みは採用できない |
| マスタのローカル一時編集機能 | 各利用者のマスタが分岐し、バージョン管理と衝突する。またローカルHTMLのパスワードは防御として機能しない |
| 受付リスト方式（1行貼り付けてフローが展開） | 構成は単純になるが即時性が失われる。貼り付けを用意した理由が即時性のため、3リスト直接貼り付けを採用 |
| 起票の出力ファイル投入口をBoxにする | Box Driveの同期遅延が加わるため。SharePointライブラリを使う。ただし起票UI本体の配布先はBox |

---

## 10. チャット運用の決め事

- **チャットは使い捨て、リポジトリが記憶**。設計の節目ごとに決定事項を `docs/設計.md` に落としてコミットする
- 会話が長くなったら遠慮なく新チャットを立て、PROJECT.md と関連ドキュメントを貼って復帰する
- **Codeに投げるべき作業は、チャット側（Claude）から明示的にコメントする**
  - 例: 「ここからはCodeの仕事です。次の指示文をCodeに渡してください」
  - チャットが出すのはCodeへの**短い指示文**であり、長いコードの丸写しをさせない
- 判断が要る局面はチャット、手を動かすだけならCode
